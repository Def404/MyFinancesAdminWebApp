@page
@model MyFinancesAdminWebApp.Pages.Currencies.IndexModel

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Currency[0].Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Currency[0].ShortName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Currency[0].Sign)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Currency) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ShortName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Sign)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.CurrencyId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.CurrencyId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.CurrencyId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>

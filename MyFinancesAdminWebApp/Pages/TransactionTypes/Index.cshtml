@page
@model MyFinancesAdminWebApp.Pages.TransactionTypes.IndexModel

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TransactionType[0].Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TransactionType[0].Class)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.TransactionType) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Class)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.TransactionTypeId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.TransactionTypeId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.TransactionTypeId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
